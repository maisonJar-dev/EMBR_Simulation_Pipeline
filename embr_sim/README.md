# EMBR Simulation Layer

This directory contains simulator-only ROS 2 packages, including Gazebo
resources and simulated CANopen devices.

Build and source `../embr_phys/ros2_ws` first, then build this workspace as an
overlay.
